<h1>Brands#index</h1>

<ul>
  <% @brands.each do |brand| %>
    <li>
      <%= link_to "#{brand.name} #{brand.ingredient.name}", brand %>
    </li>
  <% end %>
</ul>

<p><%= link_to 'New Brand', new_brand_path %></p>
